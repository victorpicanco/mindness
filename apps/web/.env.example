API_BASE_URL=http://localhost:3333
NEXT_PUBLIC_API_BASE_URL=http://localhost:3333
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54421
# Use the sitekey paired with the Turnstile secret configured in this Supabase environment.
# Cloudflare test sitekeys require the corresponding test secret and fail with production secrets.
# The local stack carries the always-passes test secret, so use the test sitekey below.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
